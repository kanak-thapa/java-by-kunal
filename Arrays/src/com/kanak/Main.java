package com.kanak;

public class Main {
    public static void main(String[] args) {
         //what are arrays why we need arrays
        //Q how to store rollnumber
         //syntax
        //datatype[] variable_name = new datatype[size]
        int[] rollnum = new int[10];
        //or
        int[] rnoms = {2, 3, 4, 5, 6};
        int[] ros;//declaration od arrays. ros is getting in  the stack, COMPLILE TIME
        //dynamic memory allocation
        ros = new int[10];// initialisation actually here object is being created in the memory heap, RUN TIME
//jls``````````````````````````````````````````````````````````
        //null(litersl), can be assigned to non primitive but not primitive, any ref var byDefalut it will be null

    }
}
